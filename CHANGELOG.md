# Motu Net 更新日志

## 0.5.2（2026-09-16）

- 场景库增加“删除场景”按钮，选中已保存的自定义场景后可确认删除；内置场景保留。
- 删除后回到“正常对照”，不会启动测试，重开软件也不会恢复已删除的场景；测试或更新期间暂不可删除。
- 安装时跳过内容相同的 WinDivert 驱动，避免常规升级重复替换被占用的驱动而要求重启电脑；真正需要更换被占用驱动时仍保留必要提示。

## 0.5.1（2026-09-16）

- 移除 Logo PNG 的白色背景，改为透明背景。
- 同步更新 Windows ICO 图标，去除应用和安装程序小图标的白底。

## 0.5.0（2026-09-16）

- 启动时后台自动检查 GitHub 最新版本，并在左下角增加“检查更新”按钮。
- 发现新版后可查看更新说明，选择“下载并更新”或“稍后”；自动提示会等当前测试结束。
- 更新前停止弱网测试和循环断网、恢复网络；安装包下载支持进度和取消，校验大小与 SHA256 后打开安装向导。
- GitHub 标签触发自动测试、打包并发布安装包和更新清单；用户下载和更新无需 GitHub 账号。

## 0.4.1（2026-09-16）

- 延迟断网增加“循环执行”开关，开启后重复正常联网、断网和恢复，直到点击“停止并恢复”。
- 循环状态显示当前轮次、等待和断网倒计时；每轮恢复后重新等待，两段时间均需至少 1 秒。
- 循环选项支持随场景保存、导入和导出；旧场景默认关闭循环，仍执行一次。

## 0.4.0（2026-09-16）

- 主界面改为蓝白重点风格，操作区、运行状态和网络条件层级更清晰。
- 增加蓝白、深色、青绿三套内置皮肤，选择会自动记住。
- 增加用户场景库，支持新建、保存修改和另存为；每个场景独立保存，重启后仍可使用。
- 当前参数已生效时显示“当前场景运行中”并禁用启动按钮，选择其他参数后可切换；延迟断网等待期间也不会重复重置倒计时。
- 高级选项收起重复、乱序、短时断网参数，选中断网场景时自动展开；帮助和更新日志跟随皮肤。
- 升级仅关闭安装目录内的 Motu 程序，不停止共享驱动服务；驱动被其他程序占用时支持提示重启后替换。

## 0.3.4（2026-09-15）

- 安装前清理改为真正开始安装前执行，避免用户在打开向导后重新启动旧程序导致驱动文件被占用。
- 兼容用户自定义安装目录，升级时按 Motu Net 程序名识别旧进程，不再依赖固定目录名称。

## 0.3.3（2026-09-15）

- 安装器增加 WinDivert 残留服务清理，应用异常退出后也能更可靠地替换 WinDivert64.sys。
- 引擎关闭句柄时同时解除收发阻塞，减少升级时驱动文件被占用的情况。

## 0.3.2（2026-09-15）

- 安装前自动关闭 Motu Net、Mutu Net 和 NetLab 旧进程，并等待 WinDivert 驱动释放，避免 WinDivert64.sys 被占用。
- 安装器默认请求管理员权限，兼容安装到 Program Files 以及覆盖旧版驱动文件。

## 0.3.1（2026-09-15）

- 修复安装向导误显示“开始菜单文件夹”页面的问题，直接显示可修改的实际安装目录。
- 安装升级时沿用同一 AppId 和上次安装目录；可通过“浏览”选择其他目录。

## 0.3.0（2026-09-15）

- 优化管理员权限提醒：明确提示“请先以管理员身份重启后使用”，并修复错误弹窗文字不可见问题。
- 支持测试运行中选择其他场景，并通过“切换并启动”先恢复当前网络条件，再自动启动新场景。
- 顶部增加当前运行场景和运行阶段提示，显示等待断网、断网倒计时及恢复状态。
- 延迟断网支持等待 N 秒后断网 M 秒并自动恢复，场景配置兼容旧版本导出文件。
- 安装包沿用同一应用标识和原安装目录，安装新版本时自动覆盖升级，不需要手动卸载旧版本。

## 0.2.1

- Motu Net 品牌、Logo 和场景库初版。
- 支持延迟、抖动、丢包、带宽、重复、乱序和短时断网。
